type: object
description: SUMMARY
x-schemaName: AdminRemoveRoleUsers
required:
  - users
properties:
  users:
    type: array
    description: The rbac's users.
    items:
      type: string
      title: users
      description: The user's users.
