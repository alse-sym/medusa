---
slug: /references/medusa-workflows/deleteRbacRolePoliciesWorkflow
sidebar_label: deleteRbacRolePoliciesWorkflow
sidebar_description: ""
tags:
  - rbac
  - workflow
---

import { TypeList, WorkflowDiagram } from "docs-ui"

# deleteRbacRolePoliciesWorkflow - Medusa Core Workflows Reference

This documentation provides a reference to the `deleteRbacRolePoliciesWorkflow`. It belongs to the `@medusajs/medusa/core-flows` package.

<SourceCodeLink link="https://github.com/alse-sym/medusa/blob/84ffdab81b1701b2db2ae3ff1df5f42d861308f0/packages/core/core-flows/src/rbac/workflows/delete-rbac-role-policies.ts#L14" />

## Examples

<CodeTabs group="workflow-exection">
    <CodeTab label="API Route" value="api-route">
    
```ts title="src/api/workflow/route.ts"
import type {
  MedusaRequest,
  MedusaResponse,
} from "@medusajs/framework/http"
import { deleteRbacRolePoliciesWorkflow } from "@medusajs/medusa/core-flows"

export async function POST(
  req: MedusaRequest,
  res: MedusaResponse
) {
  const { result } = await deleteRbacRolePoliciesWorkflow(req.scope)
    .run({
      input: {
        "role_policy_ids": [
          "{value}"
        ]
      }
    })

  res.send(result)
}
```

    </CodeTab>
    <CodeTab label="Subscriber" value="subscriber">
    
```ts title="src/subscribers/order-placed.ts"
import {
  type SubscriberConfig,
  type SubscriberArgs,
} from "@medusajs/framework"
import { deleteRbacRolePoliciesWorkflow } from "@medusajs/medusa/core-flows"

export default async function handleOrderPlaced({
  event: { data },
  container,
}: SubscriberArgs < { id: string } > ) {
  const { result } = await deleteRbacRolePoliciesWorkflow(container)
    .run({
      input: {
        "role_policy_ids": [
          "{value}"
        ]
      }
    })

  console.log(result)
}

export const config: SubscriberConfig = {
  event: "order.placed",
}
```

    </CodeTab>
    <CodeTab label="Scheduled Job" value="scheduled-job">
    
```ts title="src/jobs/message-daily.ts"
import { MedusaContainer } from "@medusajs/framework/types"
import { deleteRbacRolePoliciesWorkflow } from "@medusajs/medusa/core-flows"

export default async function myCustomJob(
  container: MedusaContainer
) {
  const { result } = await deleteRbacRolePoliciesWorkflow(container)
    .run({
      input: {
        "role_policy_ids": [
          "{value}"
        ]
      }
    })

  console.log(result)
}

export const config = {
  name: "run-once-a-day",
  schedule: "0 0 * * *",
}
```

    </CodeTab>
    <CodeTab label="Another Workflow" value="another-workflow">
    
```ts title="src/workflows/my-workflow.ts"
import { createWorkflow } from "@medusajs/framework/workflows-sdk"
import { deleteRbacRolePoliciesWorkflow } from "@medusajs/medusa/core-flows"

const myWorkflow = createWorkflow(
  "my-workflow",
  () => {
    const result = deleteRbacRolePoliciesWorkflow
      .runAsStep({
        input: {
          "role_policy_ids": [
            "{value}"
          ]
        }
      })
  }
)
```

    </CodeTab>
  </CodeTabs>

## Steps

<WorkflowDiagram workflow={{"name":"deleteRbacRolePoliciesWorkflow","steps":[{"type":"step","name":"deleteRbacRolePoliciesStep","description":"","link":"../../../Steps_Rbac/functions/core_flows.Rbac.Steps_Rbac.deleteRbacRolePoliciesStep/page.mdx","depth":1}]}} />

## Input

<TypeList types={[{"name":"DeleteRbacRolePoliciesWorkflowInput","type":"[DeleteRbacRolePoliciesWorkflowInput](../../../../types/core_flows.DeleteRbacRolePoliciesWorkflowInput/page.mdx)","optional":false,"defaultValue":"","description":"","expandable":false,"children":[{"name":"role_policy_ids","type":"`string`[]","description":"","optional":false,"defaultValue":"","expandable":false,"children":[]}]}]} expandUrl="https://docs.medusajs.com/learn/fundamentals/data-models/manage-relationships#retrieve-records-of-relation" openedLevel={1} sectionTitle="deleteRbacRolePoliciesWorkflow"/>
